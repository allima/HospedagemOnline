@model IEnumerable<HospedagemOnline.Models.Quarto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Inserir")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Numero)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Disponivel)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estabelecimento.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TipoQuarto.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Numero)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Disponivel)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estabelecimento.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TipoQuarto.Descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Alterar", new { /* id=item.PrimaryKey */ }) |
           
            @Html.ActionLink("Delete", "Excluir", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
